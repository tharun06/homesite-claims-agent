# Backend API (FastAPI + SQLModel + WebSocket).
# Pinned to the versions this project was built and tested against, so container
# builds are reproducible.
fastapi==0.138.1
uvicorn[standard]==0.49.0
sqlmodel==0.0.39
python-jose[cryptography]==3.5.0
python-multipart==0.0.32

# faker is a RUNTIME dependency, not dev-only: the app auto-seeds an empty
# database on startup (see app/main.py::_seed_if_empty).
faker==40.23.0

# PostgreSQL driver (deployed). SQLite needs no driver, so this is only
# exercised when DATABASE_URL points at Postgres.
psycopg[binary]==3.3.4
