# Copilot service (LangGraph agent + MCP server).
#
# Every version here is pinned to what this project was built and tested against.
#
# The langgraph.* family MUST be pinned as a set. Pinning only `langgraph` lets
# pip resolve `langgraph-prebuilt` to a newer release, and the container then dies
# at import with:
#     from langgraph.runtime import ExecutionInfo, ServerInfo  -> ImportError
# It works locally only because the local env happens to hold a compatible pair.
langgraph==1.0.10
langgraph-prebuilt==1.0.8
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langgraph-checkpoint-postgres==3.1.0
langgraph-sdk==0.3.9

langchain==1.2.10
langchain-core==1.4.8
langchain-openai==1.3.3

# MCP SDK v2 renames FastMCP -> MCPServer, which breaks mcp_server.py's import.
# Stay on 1.x until we migrate deliberately.
mcp>=1.28,<2
langchain-mcp-adapters==0.3.0

fastapi==0.138.1
uvicorn[standard]==0.49.0
httpx==0.28.1
python-dotenv==1.2.1
aiosqlite==0.22.1
psycopg[binary]==3.3.4
azure-monitor-opentelemetry==1.8.9
